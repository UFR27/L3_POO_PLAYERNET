# Configuration

in the `src/main/resources/application.properties`, find the following lines:

```properties
camel.component.jms.connection-factory.user=
camel.component.jms.connection-factory.password=
camel.component.jms.connection-factory.brokerURL=tcp://broker.miage.dev:80
camel.component.jms.connection-factory=#class:org.apache.activemq.artemis.jms.client.ActiveMQJMSConnectionFactory

```

and set the user/password provided in class
